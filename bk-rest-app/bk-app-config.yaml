apiVersion: v1
kind: ConfigMap
metadata:
  name: bk-app-config
  labels:
    app: bk-app-config
data:
  DB_HOST: "10.102.179.0"
  DB_PORT: "5432"
  DB_USERNAME: "postgres"
  DB_PASSWORD: "password"
  DB_NAME: "postgres"
  SSL_MODE: "disabled"
  JWT_SECRET: "test"
